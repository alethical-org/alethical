# Review the Campaign money section — round six

Aug 18 2026 · nothing to build · review order below · replaces round five's copies

Your three round-five fixes are applied and your correction of record is in the facts file. Build truth
wins over every drawing here: where the code contradicts us, say so and I redraw.

## What changed since round five

- **The byline is a placeholder again** — "ALETHICAL RESEARCH · AUTHOR NAMED AT PUBLISH". It had been bound
  to the mock's signed-in account, so every reader would have seen themselves as the author. Whose name
  signs a report stays open.
- **The PDF rule is back, beside the control it governs:** the PDF is this page, regenerated at each publish
  — one current copy per published version, never a second document maintained beside the page.
- **Paid under a name gained its not-found state** (rail state "Nothing filed under it"): reached by a typed
  or forwarded address, it says nothing is filed under that name as spelled, keeps the spellings-vary line,
  offers no nearest match, and drops the coverage line — there is no coverage to date. Fourteen states
  across the four list screens.
- **The route record is corrected to the ruling, not a measurement.** No forward is owed because the old app
  has no real users and comes down once this ships (#1667). The facts file now records that the old app is
  live at alethicalfinance.com, publishes all twelve addresses plus `/money-map`, `/legislators` and
  `/candidates-2026`, and carries the warning that neither result proves anything: it answers every address
  including a made-up one, and a 404 on alethical.com says nothing about an app hosted elsewhere.

## Read in this order

1. **Campaign money IA.dc.html** — the plan and the landing.
2. **Campaign money map.dc.html** — every route, its status, and the file that draws it.
3. **Money lists web.dc.html** — four screens on the rail: committees list, search results, all payments,
   paid under a name.
4. **Money committee web.dc.html** — the deep record, ten states.
5. **Money report web.dc.html** — the shelf and the report, with corrected and newer-filings states.
6. **LIVE Legislator profile web.dc.html** and **LIVE Legislator profile mobile.dc.html** — money as a tab
   on a page that already ships.
7. **build-facts-campaign-money.md** — the measured facts, pointing at census #1661 and your endpoint pins,
   plus your findings and the route ruling recorded as build truth.

## What I want from you

1. **Anything the data cannot support.** Still the one class of finding that stops the section.
2. **Anything asserting a route, page, count or label is live when it is not** — including a check made
   against the wrong host. You caught both instances of this so far; it is the class of error I am worst at.
3. **Drift between the settled rules and the drawings.** Cheap greps: a surviving "vendor", a pasted count,
   a threshold sentence missing "in total", a person row outside the 200, a sum across one person's
   committees, an approved-expenditure mention, anything net of refunds, a figures-disagree state naming a
   direction, a street address, a scope word on a ballot-question filer, a value bound to the signed-in
   account that should be content.
4. **Whether the four list screens are now complete enough to build against**, or what a builder still has
   to invent on the way to first release.
5. **States still missing** beyond loading, empty, capped, no-match, not-found, corrected and
   newer-filings.

## How to reply

Verdict first, in two lines: what is true, and what to do about it. Then blockers, then everything else,
clearly separated. For any factual claim about how something is built, cite the file, the function, and a
pinned commit.

## What not to spend time on

Your round-three work list is recorded and is yours, not a finding for us to answer: the committees-list
service, the name-search service on #1486, the payments-route additions, and the per-name route that already
exists. Pagination mechanics stay with the build. The Who got paid browse index remains after-first-release,
and the report's technical version still waits on its document — its link is not drawn until that page
exists. The homepage placement is deliberately open, and whose name signs a report is deliberately open.
