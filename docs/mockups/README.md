# Temporary sign-in design record

Only `sign-in/` remains here while the separate sign-in redesign is active. Do not
add another design bundle to `docs/`.

Design previews, screenshots, copied assets, and handoff notes are working files.
Keep them in the design task, its pull request, or temporary storage. Before a build
lands, move every lasting product rule into the feature's guide under
`docs/product-onboarding/`, move shared visual rules into
`docs/design/design-principles.md`, and put exact values in the frontend code.

When the sign-in redesign finishes, reconcile its lasting rules into
`docs/product-onboarding/sign-in-guide.md` and remove this final temporary folder.
