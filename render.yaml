services:
  - type: web
    name: alethical-api
    runtime: python
    plan: free
    region: virginia
    buildCommand: uv sync --frozen
    preDeployCommand: uv run python scripts/bootstrap_db.py && uv run python scripts/load_sample_data.py
    startCommand: uv run uvicorn alethical.api.main:create_app --factory --host 0.0.0.0 --port $PORT --proxy-headers --forwarded-allow-ips='*'
    healthCheckPath: /healthz
    autoDeployTrigger: commit
    buildFilter:
      paths:
        - alembic/**
        - alethical/**
        - prototype-output/**
        - prototypes/**
        - scripts/**
        - tests/**
        - pyproject.toml
        - uv.lock
        - .python-version
        - render.yaml
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.11
      - key: UV_VERSION
        value: 0.10.2
      - key: ALETHICAL_CORS_ORIGINS
        value: https://alethical-web.vercel.app,http://localhost:19006,http://127.0.0.1:19006
      - key: DATABASE_URL
        sync: false
      - key: SUPABASE_URL
        sync: false
      - key: SUPABASE_PUBLISHABLE_KEY
        sync: false
